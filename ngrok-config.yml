version: "2"
authtoken: YOUR_NGROK_TOKEN

tunnels:
  gadingpro-backend:
    proto: http
    addr: 5000
  gadingpro-frontend:
    proto: http
    addr: 5173
  gadingpro-admin:    # <-- TAMBAHKAN INI
    proto: http
    addr: 3000 

    # ngrok start --all --config=ngrok-config.yml